# 苏州大学网课自动播放

脚本：`polymas-auto-next.user.js`（v1.2.2），适用域名为 `hike-teaching-center.polymas.com`。

## 安装和使用

1. 在 Chrome 的 Tampermonkey（油猴）或 ScriptCat（脚本猫）中，选择“添加新脚本”；已安装旧版时直接编辑原脚本。
2. 删除默认模板，把 `polymas-auto-next.user.js` 的全部内容粘贴进去，按 `⌘S` 保存。
3. 刷新课程页面，右下角出现“视频自动续播”面板即表示脚本已运行。
4. 展开课程章节，从目录**手动点击一节视频**，让脚本记录当前课程。进入视频页后，脚本会自动尝试点击中央播放按钮并启动播放。
5. 视频正常播放结束后，脚本优先点击页面的“下一节”等按钮。独立详情页没有此按钮时，会保存课程状态、点击左上角“返回”，再按目录顺序打开下一条 `.mp4` 资源并尝试播放。

返回后如果网站显示“0 个学习资源 / 暂无数据”，脚本会等待 2.5 秒并自动刷新一次，然后继续定位下一节；每次续播只自动刷新一次。

更新旧版时，用新版全部内容替换原脚本、保存并刷新页面。直接进入或刷新 `resource-detail` 视频详情页也会自动尝试启动当前视频；要记录后续课程顺序，仍建议从展开的目录进入。

“暂停续播”关闭自动启动和自动切换，不暂停正在播放的视频。脚本不会反复拉起已开始播放后被你手动暂停的视频。若浏览器拦截有声自动播放，面板会显示“点击播放”，需要真实点击以授予播放权限；脚本点击无法保证绕过浏览器限制。

没有油猴时，也可以在课程页的开发者工具 Console 中执行整个脚本，临时使用；刷新页面后失效。遇到浏览器的粘贴警告，请先检查脚本内容。

## 当前适配范围

- 根据截图中的 `.mp4` 课程标题和常见播放器结构实现。由于浏览器连接超时，**尚未在你的真实页面验证 DOM 选择器及切换行为**。
- 支持当前页面及同源 iframe 中的 HTML5 `video`，以及页面动态替换播放器。
- 应在当前目录内展开后续章节；折叠、虚拟列表或翻页中尚未显示的资源可能无法识别。标题重复的课程、特殊图标或自定义播放器需要进一步适配。
- 识别到下一节锁定时，最多等待 90 秒让平台正常解锁；仍不可用会显示提示。未知样式的锁由平台自身限制处理。
- 自动化只依据视频的正常结束事件切换，不修改进度、不提交学习完成数据、不更改播放速度。
- 不支持跨域 iframe 内的播放器。当前课程、目录顺序和待续播状态保存在当前标签页的会话存储中，因此整页跳转后仍可续接；关闭标签页后会清除。
- 返回目录后若当前课程或下一章处于折叠状态，请展开对应章节，脚本会继续查找。折叠、虚拟列表或翻页中尚未显示的资源不能直接识别。
- 若另一个插件也启用了自动切课，建议只保留一个插件负责续播，避免重复切换。

## 页面结构不匹配时

可编辑脚本开头的 `CONFIG`：

```js
nextButtonSelector: '',  // 实际“下一节”按钮的 CSS 选择器
resourceRowSelector: '', // 每一行视频资源的 CSS 选择器
startButtonSelector: '.vjs-big-play-button, .prism-big-play-btn, .dplayer-play-icon, .xgplayer-start, xg-start, .plyr__control--overlaid', // 中央播放按钮，可替换为实际选择器
```

留空时自动识别。若提示未识别或切换失败，可提供播放器打开后的页面截图，以及对应按钮或资源行的 HTML，以便精准适配。

临时彻底卸载当前页面中的脚本：

```js
window.__POLYMAS_AUTO_NEXT__?.stop();
```

长期停用请在油猴中关闭该脚本。
